import React from 'react';
import SosMonacoDiff from './sos-monaco-diff.component';

const SosMonacoDiffContainer = (props) => {
	return <SosMonacoDiff {...props} />;
};

export default SosMonacoDiffContainer;
