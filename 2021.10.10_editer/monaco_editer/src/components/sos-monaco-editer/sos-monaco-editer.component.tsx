import React from 'react';
import { MainProps } from './index.model';

const SosMonacoEditer = (props: MainProps) => {
	return (
		<div className="sos-monaco-editer">
			<h1>123</h1>
		</div>
	);
};

export default SosMonacoEditer;
