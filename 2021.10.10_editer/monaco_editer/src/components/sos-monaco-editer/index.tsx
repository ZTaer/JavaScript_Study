import React from 'react';
import SosMonacoEditer from './sos-monaco-editer.component';

const SosMonacoEditerContainer = (props) => {
	return <SosMonacoEditer {...props} />;
};

export default SosMonacoEditerContainer;
