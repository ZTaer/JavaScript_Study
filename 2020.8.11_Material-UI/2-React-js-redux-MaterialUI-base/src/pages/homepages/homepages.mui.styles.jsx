import { FormHelperText } from '@material-ui/core';
import {  
    makeStyles,
} from '@material-ui/core/styles';

export const homepagesUseStyles = makeStyles({
    proItem: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        padding: "1rem",
    }
});